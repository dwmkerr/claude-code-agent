apiVersion: v1
kind: Service
metadata:
  name: {{ include "claude-code-agent.fullname" . }}
  labels:
    {{- include "claude-code-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "claude-code-agent.selectorLabels" . | nindent 4 }}
