apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "claude-code-agent.fullname" . }}
  labels:
    {{- include "claude-code-agent.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "claude-code-agent.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "claude-code-agent.selectorLabels" . | nindent 8 }}
    spec:
      # IMPORTANT: Disable service account token - the agent should NOT have access
      # to the host Kubernetes cluster. It creates isolated Kind clusters for testing.
      automountServiceAccountToken: false
      containers:
        - name: {{ .Chart.Name }}
          image: "{{- if .Values.image.tag }}{{ .Values.image.repository }}:{{ .Values.image.tag }}{{- else }}{{ .Values.image.repository }}{{- end }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if or .Values.claude.mcpConfigMap .Values.claude.args }}
          args:
            - "--"
            {{- if .Values.claude.mcpConfigMap }}
            - "--mcp-config"
            - "/config/mcp-config.json"
            {{- end }}
            {{- range .Values.claude.args }}
            - {{ . | quote }}
            {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: 2222
              protocol: TCP
          env:
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  {{- if .Values.apiKeySecret }}
                  name: {{ .Values.apiKeySecret }}
                  {{- else }}
                  name: {{ include "claude-code-agent.fullname" . }}-api-key
                  {{- end }}
                  key: api-key
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
          {{- if or .Values.claude.claudeMdConfigMap .Values.claude.mcpConfigMap .Values.claude.skills .Values.extraVolumeMounts }}
          volumeMounts:
            {{- if .Values.claude.claudeMdConfigMap }}
            - name: claude-md
              mountPath: /home/claude-code-agent/.claude/CLAUDE.md
              subPath: CLAUDE.md
              readOnly: true
            {{- end }}
            {{- if .Values.claude.mcpConfigMap }}
            - name: mcp-config
              mountPath: /config/mcp-config.json
              subPath: mcp-config.json
              readOnly: true
            {{- end }}
            {{- range .Values.claude.skills }}
            - name: skill-{{ .name }}
              mountPath: /home/claude-code-agent/.claude/skills/{{ .name }}
              readOnly: true
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
      {{- with .Values.extraContainers }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if or .Values.claude.claudeMdConfigMap .Values.claude.mcpConfigMap .Values.claude.skills .Values.extraVolumes }}
      volumes:
        {{- if .Values.claude.claudeMdConfigMap }}
        - name: claude-md
          configMap:
            name: {{ .Values.claude.claudeMdConfigMap }}
        {{- end }}
        {{- if .Values.claude.mcpConfigMap }}
        - name: mcp-config
          configMap:
            name: {{ .Values.claude.mcpConfigMap }}
        {{- end }}
        {{- range .Values.claude.skills }}
        - name: skill-{{ .name }}
          configMap:
            name: {{ .configMap }}
        {{- end }}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
